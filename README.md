# End to end machine learing project
# How to create a Envirnonment
    - Command:"conda create -p venv python==3.8 -y"
    # How to Activate Environment
        - conda activate venv/
# Github
- echo "# MLProject" >> README.md
- git init
- git add README.md
- git commit -m "first commit"
- git branch -M main
- git remote add origin https://github.com/abhayT8010273780/MLProject.git
- git push -u origin main
# How to set git global username AND email
    - URL: https://git-scm.com/book/ms/v2/Getting-Started-First-Time-Git-Setup
    - $ git config --global user.name "John Doe"
    - $ git config --global user.email johndoe@example.com
# How to create a gitignore file
    - ".gitignore"
    - git pull
